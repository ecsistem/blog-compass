name: Docker Compose Deployment

on:
  push:
    branches:
      - main
  workflow_dispatch: # Permite acionar manualmente a pipeline

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Docker Compose
      run: |
        # Instalar o Docker Compose
        sudo apt-get update
        sudo apt-get install -y docker-compose

    - name: Build and Deploy
      run: |
        # Definir o nome do projeto como 'blog-compass'
        export COMPOSE_PROJECT_NAME=blog-compass

        # Construir e iniciar os serviços usando o docker-compose
        docker-compose up -d --build

    - name: Clean up
      run: |
        # Remover os contêineres que não estão em uso
        docker system prune -f

    - name: Check Service Status
      run: |
        # Verificar o status dos serviços criados pelo docker-compose
        docker-compose ps
